function [ frame_buffer, z_buffer ] = render_sphere( n, vertex_list, vertex_color, frame_in, z_in )
% This function takes as input 
% n >> parameter used with command sphere
% vertex_list is column vector that contains all sphere vertices
% vertex_color is column vector that contains all vertices colors

% number of horizontal lines
hz = n + 1;

% Extracting horizontal lines


end

